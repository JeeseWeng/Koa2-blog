const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    HOST: '139.199.204.30',
    PORT: '3306',
    USERNAME: 'root',
    PASSWORD: 'w+*mysql',
    DATABASE: 'koa2blog',
  }
}

module.exports = config