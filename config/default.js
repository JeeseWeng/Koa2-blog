const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    HOST: 'localhost',
    PORT: '3306',
    USERNAME: 'root',
    PASSWORD: '123456',
    DATABASE: 'nodesql',
  }
}

module.exports = config